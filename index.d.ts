  /**
   * Simple sync conversion from tif to png.
   *
   * @param {Uint8Array} tif the binary tif image data
   * @returns {Uint8Array} the png image data
   */
export declare function tif2png(tif:Uint8Array): Uint8Array